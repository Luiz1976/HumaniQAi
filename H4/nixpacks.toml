[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev --no-audit --no-fund || npm install --omit=dev --no-audit --no-fund"]

[phases.build]
cmds = ["echo 'Skipping build phase for backend'"]

[start]
cmd = "node server-standalone.cjs"

